# These names are consumed by scripts/write-env.mjs during a Vercel build.
# The publishable key is intended for browser use. Never put service-role,
# secret, or database credentials in frontend environment variables.

SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
SUPABASE_PUBLISHABLE_KEY=YOUR_SUPABASE_PUBLISHABLE_KEY
